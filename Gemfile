source "https://rubygems.org"

# Pinned to the same gem GitHub Pages' legacy build uses server-side, so a
# local `bundle exec jekyll serve` matches what actually deploys.
gem "github-pages", group: :jekyll_plugins

# Ruby >= 3.0 dropped webrick from the standard library; `jekyll serve` needs it.
gem "webrick"
